PORT=5000
MONGO_URI=mongodb://127.0.0.1:27017/shopkart
JWT_SECRET=replace_this_with_a_long_random_secret
CLIENT_URL=http://localhost:5173

# Payment is currently mocked (no real gateway) — no keys needed.
# To switch to a real gateway later, add its keys here and restore
# the real create/verify logic in controllers/orderController.js
